#!/bin/bash

#This script replaces #placeholders in ans.template with the values found in Vagrantfile.yml

eval $(./parse_yaml Vagrantfile.yml)

eval "cat <<EOF
$(<ans.NAT.template)
EOF
" 2> /dev/null > ans.txt

